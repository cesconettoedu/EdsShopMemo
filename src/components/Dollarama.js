import { StyleSheet, Text, View, FlatList} from "react-native";

//import Task from './Task';

function Dollarama() {


  return (

    <View >
      <Text >Dollarama Only</Text>
      
      {/* <FlatList
        style={styles.flatList}
        data={allList}
        renderItem={({ item }) => (
          <Task data={item}/> 
        )} 
      />  */}
    </View>
  )
}

export default Dollarama

const styles = StyleSheet.create({


})
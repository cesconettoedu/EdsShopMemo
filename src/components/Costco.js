import { StyleSheet, Text, View, FlatList} from "react-native";

//import Task from './Task';

function Costco() {


  return (

    <View >
      <Text >Costco Only</Text>
      
      {/* <FlatList
        style={styles.flatList}
        data={allList}
        renderItem={({ item }) => (
          <Task data={item}/> 
        )} 
      />  */}
    </View>
  )
}

export default Costco

const styles = StyleSheet.create({


})
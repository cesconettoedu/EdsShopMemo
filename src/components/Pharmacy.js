import { StyleSheet, Text, View, FlatList} from "react-native";

//import Task from './Task';

function Pharmacy() {


  return (

    <View >
      <Text >Pharmacy Only</Text>
      
      {/* <FlatList
        style={styles.flatList}
        data={allList}
        renderItem={({ item }) => (
          <Task data={item}/> 
        )} 
      />  */}
    </View>
  )
}

export default Pharmacy

const styles = StyleSheet.create({


})